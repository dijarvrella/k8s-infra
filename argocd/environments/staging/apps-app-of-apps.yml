apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: k8s-infra-apps
  namespace: argocd
  labels:
    app.kubernetes.io/name: k8s-infra-apps
    environment: staging
spec:
  project: morichal-ai-staging
  source:
    repoURL: https://github.com/dijarvrella/k8s-infra
    targetRevision: main
    path: apps-staging
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

